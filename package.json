{
  "name": "cba-presentation",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist deck present && mkdir -p dist/deck dist/present && cp index.html dist/index.html && cp index.html dist/deck/index.html && cp present.html dist/present.html && cp present.html dist/present/index.html"
  },
  "dependencies": {},
  "devDependencies": {}
}
